# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: vue-starter-azd
services:
    web:
        project: ./server
        language: dotnet
        host: containerapp
        module: app/web
        docker:
            path: ./Dockerfile
            context: ..
            platform: amd64
